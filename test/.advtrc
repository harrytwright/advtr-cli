version: '1.0'

services:
  binary:
    name: advtr-cli
    dockerfile: .
    default_tag: latest
    build_arguments:
      - NPM_TAG=npm
    before_build:
      - echo before_build
    post_build:
      - echo post_build
    post_push:
      - echo post_push
    rm: true

registry:
  docker:
